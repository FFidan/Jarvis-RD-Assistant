export function HeroResumeReading() {
  return (
    <div className="text-faint italic font-serif text-center py-8">
      Resume reading hero — coming in Phase 2 (will surface your latest{' '}
      <code className="font-mono text-[12px]">state='reading'</code> paper).
    </div>
  );
}
