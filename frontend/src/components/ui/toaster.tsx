/**
 * Re-exports sonner's Toaster with project-level defaults.
 * Mount <Toaster /> once in AppShell to enable toast notifications globally.
 */

export { Toaster } from 'sonner';
export { toast } from 'sonner';
