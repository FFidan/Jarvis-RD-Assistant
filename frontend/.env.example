# JARVIS Frontend Environment Variables
# Copy this file to .env and fill in values

# Optional: Langfuse public dashboard URL. When set, a link card appears in Settings → Providers.
# Leave empty (or omit) to hide the card entirely.
# VITE_LANGFUSE_PUBLIC_DASHBOARD=https://cloud.langfuse.com/project/your-project-id
