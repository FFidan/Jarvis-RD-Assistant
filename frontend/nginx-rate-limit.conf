# Rate limit zone for /api/* requests, 30/min per source IP. Lives in the
# http{} context — must be COPY'd to /etc/nginx/conf.d/ at build time.
# Burst tuning happens per-location in the main nginx.conf.
limit_req_zone $binary_remote_addr zone=api_zone:10m rate=30r/m;
