[[IgnoredVulns]]
id = "GHSA-qwww-vcr4-c8h2"
ignoreUntil = 2026-10-15
reason = "react-router RSC-mode CSRF. This SPA uses declarative routing only, so the RSC packages, server actions, and the affected APIs are absent and the path is unreachable. The advisory's fixed range begins at react-router 8.3.0 (a deferred major upgrade). Tracked in docs/known-residual-risks.md; remove on v8.3.0 adoption or a back-ported patched v7."
