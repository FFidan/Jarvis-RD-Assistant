# Optional heavy dependencies — install only when INSTALL_OPTIONAL=true (see Dockerfile)
# Enable via RERANKER_ENABLED=true env var once installed.
sentence-transformers>=3.2.0  # Cross-encoder reranking; >=3.2.0 required for backend="onnx" support
optimum[onnxruntime]>=1.21    # ONNX export + runtime for faster CPU inference (~30% speedup, no torch needed at inference)
onnxruntime>=1.19             # ONNX Runtime CPU execution provider
scikit-learn>=1.4             # Optional Pulse Phase 2 classifier; code falls back when absent
networkx>=3.2                 # Optional Pulse citation graph signals; code falls back when absent
# torch is pulled in transitively by sentence-transformers for the one-time ONNX export step;
# after export+cache the model runs via onnxruntime only (no torch at inference time).
# Pin torch here if you need a specific CUDA build:
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu124
