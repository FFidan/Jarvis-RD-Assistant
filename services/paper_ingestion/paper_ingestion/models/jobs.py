"""Paper-ingestion-side job Pydantic models.

Currently empty: all job-queue models live in
``libs/jarvis_common/jarvis_common/jobs.py`` (shared across services).
This module exists so the ``models/`` subpackage has a predictable
domain slot when future paper-ingestion-specific job response shapes
are added.
"""
